{% extends "sidebar_layout.jinja" %}
{% from "common.jinja" import permission_button_group, breadcrumb, embed_discourse_comments %}

{% block title %}{{ title }}{% endblock %}

{% block introduction %}
    <h1>Job Postings</h1>
{% endblock %}
{% block top %}
    {{ breadcrumb([
    {'url': url('home:job-list'), 'text': 'Jobs & Appointments' },
    {'text': title }
    ]) }}
{% endblock %}

{% block content %}
    <div id="discourse-content">
        <h1>{{ title }}</h1>
        {{ description|markdown }}
    </div>
    <div class="btn-group">
        {% if has_change_perm %}
            <a type="button" class="btn btn-primary btn-sm" href="{{ url('home:job-update', pk=id) }}">Edit</a>
        {% endif %}
        {% if has_delete_perm %}
            <a type="button" class="btn btn-danger btn-sm" href="{{ url('home:job-detail', pk=id) }}">Delete</a>
        {% endif %}
    </div>
{% endblock %}

{% block sidebar %}
    <div class="card-metadata">
        <div class="card-block">
            <small>
                <b class="label">Contact</b>
                <br>
                <b class="label">Date Published</b>
                <p class="info">{{ date_created }}</p>
                <b class="label">Last Updated</b>
                <p class="info">{{ last_modified }}</p>
                <b class="label">Views</b>
                <br>
                <b class="label">Status</b>
            </small>
        </div>
    </div>
    <div class="card-metadata">
        <div class="card-block">
            <b>Share</b>
            <span class="col-2 fa fa-linkedin-square"></span>
            <span class="col-2 fa fa-facebook-square"></span>
            <span class="col-2 fa fa-twitter-square"></span>
            <span class="col-2 fa fa-envelope-square"></span>
        </div>
    </div>
{% endblock %}

{% block bottom %}
    <div class="col-12">
        <div id="discourse-comments"></div>
    </div>
{% endblock %}

{% block js %}
    {{ embed_discourse_comments(true) }}
{% endblock %}
