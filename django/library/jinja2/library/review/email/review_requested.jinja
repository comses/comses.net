{% set release = review.codebase_release %}

A model peer review was requested for [{{ release.title }}]({{ build_absolute_uri(release.share_url) }}) and needs [to be processed]({{ build_absolute_uri(url('library:peer-review-dashboard'))}}).
