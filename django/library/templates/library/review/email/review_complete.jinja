{% set release = review.codebase_release %}

Dear {{ review.submitter.name }},

The peer review of your release [{{ release.title }}]({{ release.get_absolute_url() }}) is complete. Your
release is now marked as peer reviewed.