{% from "common.jinja" import search_tag_href %}
{% set release = invitation.review.codebase_release %}
{% set codebase = release.codebase %}

Dear {{ invitation.invitee }},

You have been requested to review the model [{{ codebase.title }}]({{ build_absolute_uri(release.share_url) }}).

To accept or decline this invitation click [here]({{ build_absolute_uri(invitation.get_absolute_url()) }}").